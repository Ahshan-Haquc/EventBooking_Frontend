import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['via.placeholder.com','images.pexels.com', 'picsum.photos', 'dummyjson.com','placeimg.com'],
  },
};

export default nextConfig;
